---
title: "plotting"
author: "Maya Sollen-Norrlin"
date: "2023-02-22"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


## Load libraries
```{r}
library(tidyverse)
library(gridExtra)

```

## Read in data
```{r}

tidyData <- read.csv("chemistry-data-tidy.csv")

```

```{r}
glimpse(tidyData)

```
```{r}

summary(tidyData)

```

Renaming columns so they're easier to write.
```{r}

tidyData<- tidyData %>% 
  rename(
   "NH4 (mg/l)" = NH4..mg.l.,
   "TON (mg/l)" = TON..mg.l.,
   "K (ppm)" = K..ppm.,
   "NO2 (mg/l)" = NO2..mg.l.,
   "NO3 (mg/l)" = NO3..mg.l.,
   "Conductivity (mV)" = Conductivity..mV.,
   "Moisture (%)" = Moisture....
  )

head(tidyData)


```
```{r}

summary(tidyData)
ncol(tidyData)
```

```{r}


ggplot(tidyData, aes(`K (ppm)`)) +
  geom_histogram(binwidth = 1)
```

```{r}


p <- ggplot(tidyData, aes(x = Treatment, y = `K (ppm)`, fill = as.factor(Weeks))) +
  geom_boxplot(alpha = 0.5) +
  theme_bw(base_size = 15) +
  labs(fill="Time (weeks)") 

p


```


Make boxplots
```{r}


for(i in 2:9){
  plot <- ggplot(tidyData, aes(x = Treatment, y = tidyData[,i], fill = as.factor(Weeks))) +
          geom_boxplot(alpha = 0.5) + 
          ylab(colnames(tidyData[i])) +
          labs(fill = "Time (weeks)" ) +
          scale_y_continuous(limits = c(min(tidyData[,i]), max(tidyData[,i])))
 print(plot)
 ggsave(file = paste0("boxplot_", colnames(tidyData[i]), ".png"), plot = plot)
}

#grid.arrange(grobs = plots, ncol = 2)
```
 
```{r}

ggsave()
```

```{r}


combined_fig <- ggarrange(ggplot_6, ggplot_7, ggplot_8, ggplot_9, ggplot_10, ggplot_11, ggplot_12, common.legend = TRUE, legend = "bottom")


combined_fig
 
 
``` 
 
```{r}
ggplot_6

```

possible other way to di it?
```{r}

my_plots_list <- lapply(target_variables, function(each_variable) {
  ggplot(df, aes_string(each_variable)) + # NOTE - aes_string rather than aes
    geom_histogram(binwidth = 0.05) +
    ggtitle(str_c("Distribution of ",each_variable)) +  # Title of the plot
    geom_vline(xintercept = mean_val, color = "blue", lty = "dashed")
})

gridExtra::grid.arrange(grobs = my_plots_list, ncol = 2)


```